import { IconProp } from '@fortawesome/fontawesome-svg-core'

/**
 * @author rafaelvictor01
 * @return Tipo criado para substituir o uso direto do IconProp.
 * - Necessário para que não exista nenhuma dependência severa
 * da biblioteca de terceiros
 */
export type iconNameTP = IconProp
